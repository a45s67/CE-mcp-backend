{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "name": "ce.disassembly",
  "description": "Read bounded instructions or function metadata around a target address.",
  "inputSchema": {
    "type": "object",
    "oneOf": [
      {
        "type": "object",
        "required": ["action", "address"],
        "properties": {
          "action": {"const": "list"},
          "address": {"type": "string", "minLength": 1, "maxLength": 512},
          "instructionCount": {"type": "integer", "minimum": 1, "maximum": 500},
          "byteLimit": {"type": "integer", "minimum": 1, "maximum": 65536},
          "expectedGeneration": {"type": "integer", "minimum": 1}
        },
        "additionalProperties": false
      },
      {
        "type": "object",
        "required": ["action", "address"],
        "properties": {
          "action": {"const": "instruction"},
          "address": {"type": "string", "minLength": 1, "maxLength": 512},
          "expectedGeneration": {"type": "integer", "minimum": 1}
        },
        "additionalProperties": false
      },
      {
        "type": "object",
        "required": ["action", "address"],
        "properties": {
          "action": {"const": "function"},
          "address": {"type": "string", "minLength": 1, "maxLength": 512},
          "detail": {"type": "string", "enum": ["summary", "full"]},
          "expectedGeneration": {"type": "integer", "minimum": 1}
        },
        "additionalProperties": false
      },
      {
        "type": "object",
        "required": ["action", "address"],
        "properties": {
          "action": {"const": "previous"},
          "address": {"type": "string", "minLength": 1, "maxLength": 512},
          "count": {"type": "integer", "minimum": 1, "maximum": 100},
          "expectedGeneration": {"type": "integer", "minimum": 1}
        },
        "additionalProperties": false
      },
      {
        "type": "object",
        "required": ["action", "address"],
        "properties": {
          "action": {"const": "next"},
          "address": {"type": "string", "minLength": 1, "maxLength": 512},
          "count": {"type": "integer", "minimum": 1, "maximum": 100},
          "expectedGeneration": {"type": "integer", "minimum": 1}
        },
        "additionalProperties": false
      }
    ]
  },
  "outputSchema": {
    "type": "object",
    "required": ["action", "session"],
    "properties": {
      "action": {"type": "string", "enum": ["list", "instruction", "function", "previous", "next"]},
      "session": {"type": "object"},
      "items": {"type": "array", "items": {"type": "object"}, "maxItems": 500},
      "instruction": {"type": "object"},
      "function": {"type": "object"},
      "truncated": {"type": "boolean"},
      "artifactId": {"type": "string", "minLength": 8, "maxLength": 128}
    },
    "additionalProperties": false
  },
  "annotations": {"readOnlyHint": true, "destructiveHint": false, "idempotentHint": true}
}
